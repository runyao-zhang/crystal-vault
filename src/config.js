// 数据源路径。写死（#2 契约：加晶体 = 建子文件夹，不改脚本）。
//
// 单独一个文件、不 import 任何东西，是为了让 node 侧脚本
// （导入卡片、截图）也能引用同一个常量，不再各写一遍字面量。

export const CARDS_FOLDER = "3.资产舱/知识卡片";

// 落回 vault 的那张笔记，以及它旁边那份 pdf.js 运行时。**都在 vault 里的相对路径。**
//
// 两处（浏览器那一侧的适配层、node 那一侧的 land-vault 脚本）都要用，所以放这儿
// ——各写一遍字面量的下场是：改了一处、另一处还指着老地方，而症状是
// 「笔记更新了，但阅读器读不到运行时」这种两边都看着没错的怪事。
//
// ⚠️ 运行时**不能**写进那张笔记：内联过一次，笔记从 7,665 行涨到 30,260 行，
// Obsidian 的编辑器直接打不开它。理由见 core/pdfdoc.js 开头那一段。
export const VAULT_NOTE_PATH = "自己开发的项目/晶体库/知识卡片3D晶体库-v2.0.md";
export const VAULT_PDF_RUNTIME_PATH = "自己开发的项目/晶体库/pdf-runtime.js";
